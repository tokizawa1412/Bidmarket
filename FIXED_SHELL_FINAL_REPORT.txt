BidMarket Fixed Shell Final Repair

Base: BidMarket-ItemEscrow-UIFixedLayout-GmailLogin-COMPLETE(1).zip

Fixed:
- Restored desktop shell using a safe final override instead of restructuring the header.
- Header is compact and fixed at top.
- Legacy nav row is hidden on desktop to avoid duplicated/broken menu.
- Left sidebar and right sidebar stay fixed inside the viewport.
- Only the center dashboard content scrolls vertically.
- Dashboard top icons remain compact and text is hidden.
- Sub tabs keep: ทั่วไป / Vip / กลางไอเทม / กิจกรรม; removed คำอธิบาย.
- Applied shared clean card UI to ทั่วไป / VIP / กิจกรรม without breaking item escrow.
- Preserved กลางไอเทม system, Admin Check, Orders, uploads, disputes, Discord webhook, Gmail-only login.

Checks:
- node --check server.js passed
- node --check public_inline.js passed
