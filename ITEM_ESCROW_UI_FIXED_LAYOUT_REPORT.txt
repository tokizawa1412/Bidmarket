BidMarket Item Escrow UI + Fixed Layout Update

Base: BidMarket-ItemEscrow-DiscordWebhook-COMPLETE(1).zip

Changes applied:
1. Desktop layout
- Top header is fixed while scrolling.
- Left sidebar is fixed while scrolling.
- Right friends/activity sidebar is fixed while scrolling.
- Center content scrolls independently with proper desktop spacing.

2. Item escrow page UI
- Rebuilt the central item escrow UI with a cleaner hero section.
- Redesigned seller form, steps guide, warning box, fee preview area, and item list cards.
- Kept all existing IDs used by JavaScript: marketTitle, marketDesc, marketCategory, marketImage, marketImages, marketPrice, marketChar, marketSellerFee, marketGrid, marketMyItems.
- Existing backend flow and Discord webhook flow remain connected.

3. Login
- Header non-logged-in state now shows only Gmail login.
- Login page now shows only Gmail login and removed local username/password test login UI.

Checks:
- node --check server.js passed.
- node --check public_inline.js passed.
